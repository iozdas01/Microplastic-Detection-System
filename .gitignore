# Secrets
.env
.env.local
.env.*.local
.gcp/
*.json.key

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Scraper outputs / working files
scrapers/outputs/
*.log

# Visual brainstorming sessions
.superpowers/
.gstack/

# Git worktrees (managed by git worktree, not for committing)
.claude/worktrees/

# LinkedIn data export — contains connections' personal data + private message
# content. Parsed locally by scripts/data/linkedin_export.py. Never commit.
private/

# Per-idea machine caches — visited-URL sets, run manifests, API response caches.
# Regenerated on demand by the outreach skills; never review-worthy.
reports/*/outreach/.*

# Bulk source downloads — re-fetched on demand by the collectors in scripts/research/ and
# too large to track. The tidy extracts in data/processed/ and the JSON payloads in
# data/raw/ are the provenance record that matters.
reports/high-mix-manufacturing/research/data/raw/census-cbp/*.zip
reports/high-mix-manufacturing/research/data/raw/census-cbp/*.txt
reports/high-mix-manufacturing/research/data/raw/bls-cex/cx.series
reports/high-mix-manufacturing/research/data/raw/census-mrts/*.xlsx
reports/high-mix-manufacturing/research/data/raw/_probe/
