# Secrets
.env
.env.local
.env.*.local
.gcp/
*.json.key

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Scraper outputs / working files
scrapers/outputs/
*.log

# Visual brainstorming sessions
.superpowers/
.gstack/

# Git worktrees (managed by git worktree, not for committing)
.claude/worktrees/

# LinkedIn data export — contains connections' personal data + private message
# content. Parsed locally by scripts/data/linkedin_export.py. Never commit.
private/

# Outreach machine caches — visited-URL sets, run manifests, API response caches.
# Regenerated on demand by the outreach skills; never review-worthy.
reports/outreach/.*

# VibeCAD (nested upstream clone + installer, not part of this repo)
vibecad/

# CAD build caches (regenerated by the cad skill)
__cadgen__/
